# 配音界面修复验证指南

## 修复内容
2026-06-22 完成了配音界面的关键bug修复：

1. ✅ 修复了 VoicePreviewThread 类定义错误（严重bug）
2. ✅ 修复了界面初始化时的状态问题
3. ✅ 添加了详细的调试日志

## 如何验证修复

### 方法1: 运行简单导入测试
```bash
cd "D:\音视频综合助手"
.venv\Scripts\python.exe test_simple_import.py
```

预期输出：
```
Test 1: Import voice loader
OK - 75 languages loaded

Test 2: Import UI classes
OK - All classes imported

Test 3: Check VoicePreviewThread is independent
OK - VoicePreviewThread is properly defined

All tests passed!
```

### 方法2: 运行完整的音色加载器测试
```bash
cd "D:\音视频综合助手"
.venv\Scripts\python.exe test_voice_loader_direct.py
```

预期输出：
- 应该看到成功加载 75 种语言
- 中文有 14 个音色
- 音色名称正确显示（如：Xiaoxiao(Female/CN)）

### 方法3: 在实际应用中测试

1. 启动应用并打开配音界面
2. 检查：
   - 语言下拉框是否可见且有内容（应该有 75 个选项）
   - 选择"中文 (zh)"后，音色下拉框应该显示 14 个中文音色
   - 音色名称格式：如 "Xiaoxiao(Female/CN)"
   - 试听按钮应该可用

3. 如果仍然有问题，检查调试日志：
   - 应用会在控制台输出详细的加载信息
   - 查找包含 "开始加载语言列表"、"获取到 X 种语言" 等日志

## 已知限制

1. Edge TTS 需要网络连接来获取音色列表（首次加载）
2. 如果网络失败，会回退到静态 JSON 文件（`data/voices/edge_tts.json`）
3. ElevenLabs API 测试需要有效的 API Key

## 修复的文件

- `videocaptioner/ui/view/dubbing_interface.py`
  - 第 958-965 行：修复 VoicePreviewThread 类定义
  - 第 475-479 行：添加界面初始化修复
  - 第 640-656 行：添加 _load_languages 调试日志
  - 第 658-691 行：添加 _on_language_changed 调试日志
  - 第 574-606 行：添加 _on_provider_changed 调试日志

## 如果还有问题

运行以下命令收集调试信息：
```bash
cd "D:\音视频综合助手"
.venv\Scripts\python.exe test_voice_loader_direct.py > debug_output.txt 2>&1
```

然后查看 `debug_output.txt` 文件，寻找错误信息。
